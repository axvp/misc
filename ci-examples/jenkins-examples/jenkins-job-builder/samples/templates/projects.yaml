- project:
    name: example1

    jobs:
      - '{name}-build'

- project:
    name: example2

    jobs:
      - '{name}-build'

- project:
   name: example3
   pyver:
    - 26:
       branch_name: old_branch
    - 27:
       branch_name: new_branch
   jobs:
    - '{name}-{pyver}'
